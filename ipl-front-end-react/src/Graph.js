export default function Graph({ iplData }) {
  if (iplData != null) {
    // console.log(iplData);
    const graphOneData = iplData[0];
    const graphTwoData = iplData[1];
    console.log(graphOneData);
    console.log(graphTwoData);
    // const graphOneXasis = graphOneData.map((items) => items.includes("200"));
    // const graphOneYaxis =
  }

  return <h1></h1>;
}
